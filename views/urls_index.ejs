<!DOCTYPE html>
<html lang="en">

<head>
  <title>TinyApp</title>
</head>

<body>
  <% include partials/_header %>
  <table cellpadding="5" style="margin:50px;">
    <tr>
      <th>Original URL</th>
      <th>Shortened URL</th>
    </tr>
    <% for (let url in urls) { %>
    <tr>
      <td> <a href=<%= urls[url]["longURL"] %> ><%= urls[url]["longURL"] %> </a></td>
      <td> <a href=http://localhost:8080/u/<%= url %> >http://localhost:8080/u/<%= url %> </a></td>
      <td>
        <a href= <%= "/urls/" + url %> >Edit</a>
      </td>
      <td>
        <form method="POST" action= <%= "/urls/" + url + "?_method=DELETE" %>>
          <input type="submit" value="Delete">
        </form>
      </td>
    </tr>
    <% }; %>
  </table>
  <p style="margin:50px;"><a href="/urls/new">Create a new short URL</a></p>
</body>

</html>